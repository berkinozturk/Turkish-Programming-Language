başla

	a = 1;
	b = 2;
	c = a + b;
    yaz (c);

g değişkeni 20 ile 40 arasında 5 artarken {
    eğer (g==20) ise {
    yaz("irem");
    devam et;
    }
    ya da (g == 25) ise {
    yaz(g);
    kır;
    }
    ya da (g == 30) ise {
    yaz(g);
    yaz("kirilmadi");
    };

    };

 h=20;
 t=10;
 eğer (h==21) ise {
 yaz(g);
 }
 ya da (t==10) ise {
 yaz("görüyor mu?");
 }
 değil ise {
 yaz("olduu");
 };







bitir